defaults:
  - model: tiny
  - optimizer: adamw
  - _self_

training:
  epochs: 4
  batch_size: 64
  seed: 42
  deterministic: false

hydra:
  run:
    dir: outputs/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
